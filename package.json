{
  "name": "tml-graphql-service",
  "version": "1.0.1",
  "description": "A service that provides access to TML GraphQL API data",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "smithery:build": "npx smithery build",
    "smithery:dev": "npx smithery dev",
    "start": "node --experimental-strip-types --no-warnings src/stdio.ts",
    "dev": "node --experimental-strip-types --inspect --watch src/stdio.ts",
    "inspect": "npx @modelcontextprotocol/inspector node --experimental-strip-types src/stdio.ts",
    "test": "node --experimental-strip-types --no-warnings --test test/**/*.test.ts",
    "test:dev": "node --experimental-strip-types --no-warnings --test --watch test/**/*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "@smithery/cli": "^2.2.1",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
